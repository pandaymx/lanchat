# CLAUDE.md

> 完整工程约定见 [AGENTS.md](./AGENTS.md)。本文件是它的补充，**两者冲突时以 AGENTS.md 为准**。

@AGENTS.md

## 给 Claude 的补充约定

1. **先读 `方案.md` 再动手**。里面有当前里程碑、验收标准和尚未拍板的待定项（§13）。用户是「方案优先、按需生成」的工作方式：**默认只产出方案与文档，不要主动生成实现代码**，等用户说「生成/搭建/开始做」再写。

2. **不要修改版本号、不要手动打 tag**。版本由 semantic-release 管理，版本号通过 ldflags 注入，源码里不应该出现版本号字符串。

3. **改 `pkg/core` 的接口要格外谨慎**。它是四端共享层，且被 gomobile bind。改之前先确认四端影响面，并在 commit message 里说明。

4. **不要引入 CGO 依赖**。这会直接破坏交叉编译（Windows/macOS 桌面端）与 gomobile（移动端）。加依赖前先确认它是否需要 CGO。

5. **不要提议引入 JS 前端框架**。项目已选定 Go 全栈 templ + HTMX，这是一条已拍板的架构决策（ADR-003），不要再讨论。

6. **提交要原子化**。一次只做一件事，能独立 revert。具体规则见 AGENTS.md §5.2。

7. **verify-don't-assume**。关于代码现状的结论，必须用 grep / git log / 实际读文件来证实，不要凭印象断言。

8. **最小改动**。不顺手重构、不顺手优化、不添加用户没要求的抽象。
